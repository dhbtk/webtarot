services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Pass through to Vite build ARG in Dockerfile (frontend stage)
        VITE_SENTRY_DSN: ${SENTRY_DSN}
    environment:
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/webtarot?sslmode=disable
      - RUST_LOG=info,webtarot=debug
      - OPENAI_KEY=${OPENAI_KEY}
      - SENTRY_DSN=${SENTRY_DSN}
      - VITE_SENTRY_DSN=${SENTRY_DSN}
      - GOOG_API_KEY=${GOOG_API_KEY}
    ports:
      - "3000:3000"
    depends_on:
      - redis
      - postgres
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    command: [ "redis-server", "--appendonly", "yes" ]
    volumes:
      - redis-data:/data
    restart: unless-stopped

  postgres:
    image: postgres:18-alpine
    environment:
      - POSTGRES_DB=webtarot
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d webtarot" ]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - postgres-data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  redis-data:
  postgres-data:
  es-data:
